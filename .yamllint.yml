extends: default

rules:
  # Dependabot writes the version comment with a single space
  comments:
    min-spaces-from-content: 1
  document-start: disable
  # SHA-pinned actions with a version comment run long
  line-length:
    max: 120
  # Workflows use "on:" as a key
  truthy:
    check-keys: false
