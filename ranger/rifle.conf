# vim: ft=cfg
name ^[mM]akefile$ = make
ext 1              = man "$1"

ext pdf,         has xdg-open,  flag f = xdg-open "$@"
mime ^image,     has xdg-open,  flag f = xdg-open "$@"

ext gz,     has tar, flag f = tar xfvz "$@"
ext tar,    has tar, flag f = tar xfv "$@"

ext .* = $EDITOR "$@"
