# use alt as modifier
set $mod Mod1
font "MesloLGSDZ Nerd Font Mono 8"

# colors
set $bg         #282a36
set $selection  #44475a
set $fg         #f8f8f2
set $purple     #bd93f9
set $red        #ff5555

# Output configuration (dual 4K)
output HDMI-A-1 resolution 3840x2160@60Hz position 0,0 scale 1.5
output HDMI-A-2 resolution 3840x2160@60Hz position 2560,0 scale 1.5

# Wallpaper
set $wallpaper ~/.dotfiles/wallpaper/blurred.jpg
output * bg $wallpaper fill

# Input configuration
input type:pointer {
    accel_profile adaptive
    pointer_accel -0.2
}
input type:keyboard {
    xkb_layout us
    repeat_delay 300
    repeat_rate 50
}

seat * xcursor_theme Bibata-Original-Classic 24

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod normal

bindsym $mod+Return exec alacritty

# kill focused window
bindsym $mod+x kill

# start fuzzel (application launcher)
bindsym $mod+d exec fuzzel

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
bindsym $mod+z splith

# split in vertical orientation
bindsym $mod+n splitv

bindsym $mod+e workspace 1
bindsym $mod+Shift+e move container to workspace 1

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# toggle tiling / floating
bindsym $mod+Ctrl+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent/child container
bindsym $mod+a focus parent
bindsym $mod+Shift+a focus child

# scratchpad
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

set $ws_browser "0:󰖟"
set $ws_terminal "0:"

# switch to workspace
bindsym $mod+q workspace $ws_browser
bindsym $mod+w workspace $ws_terminal

bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5

# move focused container to workspace
bindsym $mod+Shift+q move container to workspace $ws_browser
bindsym $mod+Shift+w move container to workspace $ws_terminal

bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5

# reload sway configuration
bindsym $mod+p reload

# resize window (you can also use the mouse for that)
mode "resize" {
    # These bindings trigger as soon as you enter the resize mode
    bindsym h resize shrink width 20 px
    bindsym j resize grow height 20 px
    bindsym k resize shrink height 20 px
    bindsym l resize grow width 20 px

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+i mode "resize"

bar {
    swaybar_command waybar
}

# Machine-specific: workspace to output mappings
workspace $ws_browser output HDMI-A-1
workspace $ws_terminal output HDMI-A-2

# take a screenshot
bindsym $mod+v exec mkdir -p ~/downloads && grim -g "$(slurp)" ~/downloads/screenshot-$(date +%Y%m%d-%H%M%S).png
bindsym $mod+Shift+v exec grim -g "$(slurp)" - | wl-copy
bindsym $mod+Ctrl+v exec mkdir -p ~/downloads && grim ~/downloads/screenshot-$(date +%Y%m%d-%H%M%S).png

# move workspace to other output
bindsym $mod+Ctrl+h move workspace to output left
bindsym $mod+Ctrl+l move workspace to output right

# start application commands
bindsym $mod+Shift+c exec google-chrome-stable

bindsym $mod+y [urgent=latest] focus

# notification control
bindsym $mod+b exec makoctl dismiss
bindsym $mod+Shift+b exec makoctl restore

# floating window settings
for_window [app_id=".*"] inhibit_idle fullscreen
for_window [class=".*"] inhibit_idle fullscreen
for_window [app_id="wdisplays"] floating enable, fullscreen disable
for_window [app_id=".*blueman.*"] floating enable, fullscreen disable
for_window [app_id="org.pulseaudio.pavucontrol"] floating enable, fullscreen disable
for_window [app_id="nm-connection-editor"] floating enable, fullscreen disable

# assign to workspaces
assign [app_id="google-chrome"] workspace $ws_browser
assign [app_id="Alacritty"] workspace $ws_terminal

workspace_auto_back_and_forth yes
focus_on_window_activation smart

# other window settings
default_border none
default_floating_border pixel 1

swaynag_command swaynag --background $bg --text $fg --button-background $purple --border $selection --border-bottom $selection

#class                  border      backgr.     text        indicator   child_border
client.focused          $purple     $purple     $bg         $purple     $purple
client.focused_inactive $bg         $bg         $fg         $bg         $bg
client.unfocused        $bg         $bg         $fg         $bg         $bg
client.urgent           $red        $red        $bg         $red        $red

include /etc/sway/config.d/*

exec_always wpctl set-volume @DEFAULT_SOURCE@ 1.0
exec_always ratbagctl "hollering-marmot" resolution 1 dpi set 1600
exec_always ratbagctl "hollering-marmot" led 0 set mode cycle duration 8000 brightness 255
exec_always ratbagctl "hollering-marmot" led 1 set mode cycle duration 8000 brightness 255
exec alacritty -e fish -C 'tmux new-session -A -s work'
exec google-chrome-stable
